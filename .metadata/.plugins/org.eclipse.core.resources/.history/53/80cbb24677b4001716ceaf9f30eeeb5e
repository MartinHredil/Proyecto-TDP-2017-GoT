package juego;
import java.util.Iterator;
import disparos.*;

public class MovimientoDispPer extends Thread {

	protected volatile boolean execute;
	
	protected Juego juego;
	
	public MovimientoDispPer(Juego j)
	{
		juego=j;
	}
	
	public void terminate()
	{
		execute = false;
	}
	
	public void run()
	{
		this.execute = true;
		Iterator<DisparoPersonaje> it1;
		
		while(execute)
		{
			try {
				sleep(10);
			} catch (InterruptedException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}
			
			for(DisparoPersonaje dp:juego.getDisparosPersonajes())
			{
				
			}
			
			/*
			it1 = juego.getDisparosPersonajes();
			while(it1.hasNext())
			{
				it1.next().mover();
			}*/
		}
	}
}